config BR2_PACKAGE_LIBPNG
	bool "libpng"
	select BR2_PACKAGE_ZLIB
	help
	  Library for handling PNG (Portable Network Graphics)
	  images.

	  http://www.libpng.org/

if BR2_PACKAGE_LIBPNG

config BR2_PACKAGE_LIBPNG_14
	bool "Use libpng version 1.4 instead of 1.6"

endif
